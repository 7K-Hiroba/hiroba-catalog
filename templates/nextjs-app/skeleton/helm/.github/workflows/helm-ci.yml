name: Helm CI

on:
  pull_request:
    branches: [main]
    paths: ['helm/**']
  push:
    branches: [main]
    paths: ['helm/**']

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  helm:
    uses: 7K-Group/workflows-library/.github/workflows/ci-helm.yml@8f3f1dd841f9f34fc74a5fdb76577119b251ec0c # v1
    with:
      path: helm
